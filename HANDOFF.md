# 🌹 HANDOFF — فروشگاه «روزا» (فایل واحد انتقال به چت جدید)

## وضعیت فعلی
- **مخزن:** https://github.com/rosaashop/rosa-jewelry (شاخه main، خصوصی)
- **دیپلوی زنده:** کلودفلر Workers (اتصال Git) — هر push = دیپلوی خودکار ~۱ دقیقه
- **دیتابیس کلودفلر:** KV namespace با binding متغیر `ROSA_DB`
- **نسخهٔ لوکال/سرور شخصی:** `node server.js` (بدون وابستگی npm، پورت 3000)

## متن آمادهٔ پیست برای چت جدید
> پروژهٔ فروشگاه زیورآلات «روزا» را ادامه بده. کد کامل در **https://github.com/rosaashop/rosa-jewelry** (شاخه main) است.
> مشخصات: SPA دوزبانه فا(RTL)/ان(LTR) + سرور Node بدون وابستگی با دیتابیس JSON + نسخهٔ سرورلس کلودفلر (worker.js + wrangler.toml + functions/api). دیزاین مینیمال زنانه: سفید/گل‌بهی/صورتی ظریف/خاکستری تیره، فونت وزیرمتن+Manrope لوکال، لوگوی بزرگ در هدر و پنل لوگو در «داستان ما».
> ورود مدیر 09120000000/admin123 · مشتری 09121112233/1234 · کوپن‌ها ROZA10/WELCOME/PEARL20.
> هر تغییر بعد از push به‌صورت خودکار روی کلودفلر دیپلوی می‌شود.

## حساب‌ها و داده نمونه
مدیر `09120000000/admin123` · مشتری `09121112233/1234` · کوپن‌ها `ROZA10`,`WELCOME`,`PEARL20`
۱۷ محصول / ۶ دسته / ۵ سفارش نمونه / نظرات / FAQ / صفحات ثابت

## ساختار فایل‌ها
```
server.js      API+static نسخهٔ Node (ترتیب روت‌ها مهم: لیست‌ها شرط !r1)
seed.js        make() دیتابیس اولیه
worker.js      ورودی Worker کلودفلر: /api/* → handler، وگرنه env.ASSETS.fetch
wrangler.toml  name/main/compatibility_date + assets=public binding=ASSETS
functions/api/[[path]].js   همان API به‌صورت سرورلس، ذخیره‌سازی در env.ROSA_DB (KV)؛ آپلود رسید→dataURL
public/        index.html(?v= کش‌باست) · css/app.css · js/{i18n,api,ui,shop,admin,main}.js · assets/
```

## نکات فنی کلیدی
1. RTL/LTR با dir روی html + logical properties؛ سوییچ `setLang()` رندر کامل.
2. وزیرمتن = فایل variable با `font-weight:100 900`.
3. پرداخت card → status `awaiting_confirm` + receipt{url,tracking,status}؛ تأیید/رد مدیر با `POST /api/orders/:id/review-receipt`.
4. درگاه: ساختار آماده (merchantId در تنظیمات)؛ دمو با `POST /api/orders/:id/pay`.
5. رنگ‌ها/برند/لوگو/تعمیرات/… همه از `#/admin/settings` بدون کد.
6. کش: `no-store` + `?v=` در index.html.

## فلو انتشار (برای هر پروژهٔ بعدی)
1. ساختار: `public/` + `worker.js` + `wrangler.toml` (+اختیاری functions/, seed.js)
2. `git init -b main && git add -A && git commit` → push به ریپوی گیتهاب
3. کلودفلر: Workers & Pages → Create → **Import a repository** (نه آپلود zip — zip بدون Functions است!)
4. KV: Add namespace → آیدی آن را در `wrangler.toml` بگذار (`[[kv_namespaces]]`) تا با هر دیپلوی همراه باشد (binding فقط از داشبورد، در دیپلوی بعدی می‌پرد!)
5. دامنه اختصاصی: Settings → Domains
**آپدیت:** ویرایش → commit → push = دیپلوی خودکار.

## سهمیه رایگان کلودفلر
Workers ۱۰۰هزار درخواست/روز · KV: ۱GB ذخیره، ۱۰۰هزار خواندن و ۱هزار نوشتن/روز — کافی برای فروشگاه کوچک/متوسط؛ رشد بیشتر = پلن $5/ماه.

## وضعیت سفارش‌ها
pending_payment → awaiting_confirm → paid → preparing → shipped → delivered / canceled

## آخرین تغییر (۲۰۲۶-۰۹-۰۲) — کش‌باست `?v=23`
- دکمهٔ «افزودن به سبد» از روی تصویر کارت محصول به کنارِ مبلغ (ردیف قیمت) منتقل شد؛ کوچک‌تر (۳۴px) و همتراز با قیمت.

## آخرین تغییر (۲۰۲۶-۰۹-۰۲) — کش‌باست `?v=22`
- کارت‌به‌کارت از «تنظیمات سایت» حذف شد (فقط در «پرداخت و درگاه بانکی»؛ جلوی تکرار گرفته شد).
- دکمهٔ کوچک «افزودن به سبد» گوشهٔ تصویر هر کارت محصول (`.qadd`).
- سایدبار ادمین: دکمه‌های «تغییر رمز ورود» (مودال با تأیید رمز فعلی + `PUT /me`) و «خروج از پنل مدیریت»؛ چیپ سفید لوگو بزرگ‌تر (۵۴px).
- **ادمین → «پرداخت و درگاه بانکی»:** فعال/غیرفعال‌سازی کارت‌به‌کارت و درگاه، شرکت درگاه (زرین‌پال/آی‌دی‌پی/پی‌پینگ)، مرچنت‌کد/توکن، شماره کارت/شبا/صاحب کارت، متن راهنما — بدون کدنویسی؛ تسویه‌حساب فقط روش‌های فعال را نشان می‌دهد (`settings.payment.c2cEnabled/gatewayEnabled/provider/token`).
- **ادمین → «دستیار سئو»:** چک‌لیست ۷موردی + امتیاز + دکمهٔ «اعمال سئوی خودکار» (تکمیل متای فا/ان محصولات و متای خانه). روی سایت زنده اجرا شد → امتیاز ۱۰۰.
- **سئوی فنی:** `/sitemap.xml` (۲۷ URL) و `/robots.txt` در worker.js و server.js؛ تگ‌های OG/Twitter/canonical داینامیک؛ JSON-LD (Organization/WebSite/Product/BreadcrumbList)؛ متای استاتیک در index.html.

## آخرین تغییر (۲۰۲۶-۰۹-۰۲) — کش‌باست `?v=21`
## آخرین تغییر (۲۰۲۶-۰۹-۰۲) — کش‌باست `?v=20`
- **بهینه‌سازی حجم:** عکس‌های محصولات/بنرها با PIL بازفشرده شدند (۹۷۲K→۵۹۵K، کیفیت حفظ)؛ ویدیوی هیرو با x264 crf26 + faststart بازکدگذاری شد (۳٫۱M→۶۷۲K، فریم‌ها چشمی مقایسه و یکسان تأیید شدند).
- **پنل مدیریت موبایل:** جدول‌ها (`‏.tbl`) در موبایل `display:block; overflow-x:auto` گرفتند تا اسکرول داخلی داشته باشند + `min-width:0` برای ستون‌های گرید ادمین؛ سرریز افقی پنل صفر شد.
- **تعمیر موبایل:** دو قاعدهٔ خراب‌شدهٔ قدیمی در app.css (ادغام خطوط `.vopt/.stock/.tabs` از نسخه‌های اولیه) + زبالهٔ انتهای فایل باعث سقوط مدیاکوئری‌های موبایل شده بود؛ تعمیر شد. هدر موبایل فشرده (gap 12، آیکون ۳، پدینگ ۱۴) تا سرریز افقی صفر شود؛ `body{overflow-x:clip}` به‌عنوان قفل ایمنی.
- عکس لحظه‌ای قبل از ویدیو حذف شد (ویدیو بدون `poster`، با `preload=auto`؛ محو شدن فقط وقتی فریم اول آماده شد، کلاس `.ready` + پشتیبان ۳٫۵ ثانیه‌ای برای موبایل‌های کند).
- موبایل: ارتفاع هیرو `min(hm, 74vh)` تا صفحه را نگیرد؛ پس‌زمینه گرادیان ملایم پشت ویدیو هنگام لود.
- راستی‌آزمایی با کروم هدلس واقعی (puppeteer) در ۳۹۰px: home/shop/product/footer همگی scrollWidth=390.
- **اسلایدر کامل حذف شد**؛ سردر سایت حالا **هیروی ویدیویی سینمایی** است: `assets/video/hero.mp4` (لایسنس رایگان Mixkit — نمای نزدیک دستِ خانمی که حلقه دست می‌کند، با دسته‌گل)، پخش خودکار بی‌صدا + لوپ، زوم آهسته ۲۲ثانیه‌ای، پردهٔ گرادیان برای خوانایی متن، ورود پلکانی متن‌ها، احترام به `prefers-reduced-motion`.
- متن عنوان/زیرعنوان و ارتفاع ویدیو از ادمین → تنظیمات → «ویدیوی سردر سایت» قابل ویرایش است (`settings.hero` در KV).
- تب «اسلایدرها» از ادمین حذف شد؛ `/api/sliders` هنوز در بک‌اند هست ولی بی‌استفاده.
- ارتفاع پیش‌فرض: دسکتاپ 460 / موبایل 500 پیکسل.
