# راهنمای خیلی سادهٔ انتشار — روزا

دو مسیر داریم: **گیت‌هاب** (نگهداری کد) و **کلودفلر** (سایت زندهٔ رایگان).
فایل آمادهٔ کلودفلر: `rosa-cf.zip` (همین‌جا در ورک‌اسپیس — دانلودش کن).

---

## بخش ۱ — گیت‌هاب (۴ قدم ساده)

1. برو `github.com` و اگر حساب نداری با ایمیل‌ت Sign up کن.
2. بالای صفحه سمت راست روی **+** بزن → **New repository** → در کادر Name بنویس `rosa-jewelry` → دکمهٔ سبز **Create repository**. (Private بماند اشکالی ندارد)
3. ساخت توکن (کلید دسترسی):
   - روی عکس پروفایل‌ت بزن → **Settings**
   - پایین منوی سمت چپ → **Developer settings**
   - سمت چپ → **Personal access tokens** → **Tokens (classic)** → **Generate new token (classic)**
   - یک نام دلخواه بده، تیک گزینهٔ **repo** را بزن، پایین صفحه **Generate token**
   - توکن ساخته می‌شود (با `ghp_` شروع می‌شود) — **همان لحظه کپی‌اش کن**؛ دیگر نشان داده نمی‌شود!
4. در همین چت برایم بفرست:
   - آدرس ریپو (مثلاً `https://github.com/USERNAME/rosa-jewelry`)
   - توکن
   تا خودم همهٔ کد را push کنم. بعدش token را از همان صفحهٔ توکن‌ها **Delete** کن تا امن بماند.

> اگر روزی خواستی خودت push کنی (روی سیستم خودت):
> ```
> git clone https://github.com/USERNAME/rosa-jewelry.git
> # فایل‌های پروژه را داخلش کپی کن
> git add . && git commit -m "ROSA store" && git push
> ```

---

## بخش ۲ — کلودفلر (بدون هیچ نصبی، ۵ قدم)

1. برو `dash.cloudflare.com` و Sign up کن (پلن رایگان Free کافی است).
2. ساخت دیتابیس: از منوی سمت چپ **Workers & Pages** → تب **KV** → **Create a namespace** → نام: `ROSA_DB` → Add.
3. آپلود سایت: منوی چپ **Workers & Pages** → دکمهٔ **Create** → تب **Pages** → **Upload assets** →
   نام پروژه: `rosa` → فایل `rosa-cf.zip` را همین‌جا از ورک‌اسپیس دانلود کن و در کادر آپلود رها کن → **Deploy site**.
   (اگر zip قبول نکرد: اول zip را روی سیستم‌ت Extract کن و پوشهٔ داخلش را آپلود کن.)
4. اتصال دیتابیس: وارد پروژهٔ `rosa` شو → تب **Settings** → سمت چپ **Functions** → پایین صفحه **KV namespace bindings** → **Add binding** →
   Variable name: `ROSA_DB` → انتخاب namespace: `ROSA_DB` → Save.
   بعد تب **Deployments** → آخرین دیپلوی → **Retry deployment** (تا با binding جدید ساخته شود).
5. تمام! آدرس سایتت چیزی مثل `https://rosa.pages.dev` است.
   دامنهٔ اختصاصی: Settings → **Domains** → آدرس دامنه‌ات را بزن (اگر دامنه‌ات جای دیگری است، از Cloudflare رکوردها را می‌خواهد).

نکته‌ها:
- بار اول که سایت باز شود، دیتابیس نمونه (محصولات/مدیر/…) خودکار ساخته می‌شود.
- ورود مدیر روی کلودفلر هم همان است: `09120000000 / admin123` — بعداً از پنل تنظیمات عوضش کن… فعلاً رمز در seed است؛ برای تغییرش در نسخهٔ کلودفلری بگو تا سخت‌ترش کنیم.
- رسیدهای کارت‌به‌کارت در این نسخه داخل خود دیتابیس (KV) ذخیره می‌شوند — بدون نیاز به سرور فایل.

---

## نسخهٔ سرور شخصی (اختیاری)
اگر روزی خواستی روی VPS خودت: کل همین ورک‌اسپیس را کپی کن و `node server.js` بزن؛ دامنه را هم می‌توانی پشت CDN کلودفلر بگذاری.
